# Claude Code 技术交接 —— 《轻安 / Molt》原型

> 读这份之前，如果有 `PRD_轻安_产品与决策文档.md`，先读它拿全局。本文只讲**怎么把它实现成一个能玩的 Web 原型**。

---

## 1. 你的任务

搭一个**可玩的 Web 原型（纵向切片）**：一个等距视角的空间解谜游戏，玩家旋转/操纵不可能几何的中式建筑，引导一个负重角色走到出口。

**本期范围** = 通用引擎 + 5～6 个关卡（见 §6 里程碑与 PRD §8）。不是做完整 12 关，不是做原生 App。

---

## 2. 技术栈

- **构建**：Vite。
- **渲染**：Three.js（vanilla JS 即可；关卡 schema 若想要类型安全可选 TypeScript，非必须）。
- **相机**：`THREE.OrthographicCamera`，固定等距角，**全程不旋转、不透视**。
- **不引入物理引擎**：没有重力、碰撞体、刚体。移动是节点间的插值动画，不是物理。
- **目标平台**：先桌面浏览器（鼠标），触控适配放后面。
- **部署**：纯静态（GitHub Pages / itch.io / Vercel 均可）。

---

## 3. 核心引擎架构（最重要，先读懂再动手）

**一句话**：不要造一个"通用的不可能几何求解器"。**关卡是数据，运行时是通用的。** 每关的"不可能连接"由**手工授权的数据**描述，运行时只负责读数据、跑寻路、放动画。

原作 ustwo 之所以要从零写一套通用导航系统，是因为他们要支持大量可自由重组的几何状态、又不想每关手动标注。**作为单人项目，我们反过来**：牺牲通用性，换掉最难的工程——每关只手工设计**少数几个离散状态**，每个状态的可行走连接**手工连一遍**。

### 关键机制

1. **固定正交相机**。摆在等距角（约 azimuth 45°、俯角 35.264°，即经典 2:1 等距）。所有结构只需在这一个视角下"看起来成立"。

2. **关卡 = 静态几何 + 若干机关（mechanism）+ 手工节点图（node graph）**。
   - **机关**：可旋转/移动的一组几何，只有**有限个允许的"吸附态"**（如旋转 ∈ {0°, 90°, 180°, 270°}）。玩家交互只能把机关切到某个吸附态，不能停在中间。
   - **节点图**：角色能站的点（node）+ 可行走的连接（edge）。**导航不从 3D 邻接计算**——因为在这个世界里"屏幕上相邻"≠"3D 里相邻"。edge 是**授权的**，且可以**依赖机关状态**：某条 edge 只有当机关 M 处于状态 S 时才存在（因为只有那时几何在相机视角下才接上）。**这就是把"相机空间的邻接"编码成数据，绕开了造通用求解器。**

3. **移动**：玩家点一个"当前可达"的节点 → 在**当前激活的 edge** 上跑寻路（BFS/Dijkstra）→ 角色沿路径逐节点走动画。**允许"瞬移接缝"**：两个节点在 3D 里可能不连续，但在屏幕上连续，角色照走（纪念碑谷就是这么在接缝处做的）。

4. **交互**：拖拽/点击机关手柄 → 切换机关吸附态 → **重算激活 edge 集合 + 刷新"可达节点"高亮**（可达节点亮朱砂色；几何一变，高亮随之变）。这套高亮也顺便容错了玩家不精准的点触。

5. **胜利条件**：角色到达 `exit` 节点。

6. **"不可能"是每个状态手工摆出来的**：关卡设计者（Sally）在某个吸附态 S 下把几何摆成"3D 里很远的两块，在屏幕上恰好对齐"。可用**接缝辅助几何**（一块"半立方"补角，遮住对齐处的缝隙——即 Penrose triangle 的经典做法）。运行时无需动态解错觉。

### 渲染注意（真实的坑）

- **不要用点光源**。因为"屏幕上相邻的东西在 3D 里并不相邻"，点光源会穿帮。用**方向光 + 环境光**，阴影/体积感靠**烘焙或顶点 AO（环境光遮蔽）**来做。这是纪念碑谷做出"厚重感"又不穿帮的关键。
- 材质走扁平/低多边形 + 柔和光照。别上 PBR 写实。风格 token 从 Design 拿（见 §7）。
- 最终可把大量 quad 合并成 combined mesh 以保证性能（后期优化，原型阶段不必）。

---

## 4. 关卡数据模型（Schema + 完整示例）

关卡是纯数据。运行时读它即可跑。下面是最小 schema 与一个**完整可跑的示例：〇·门**。

### Schema（字段说明）

```jsonc
{
  "id": "string",                       // 关卡唯一 id
  "camera": { "azimuthDeg": 45, "elevationDeg": 35.264 },
  "palette": "ink" ,                    // 引用 Design tokens 里的某套色（如 ink / celadon / white）
  "geometry": [                         // 静态几何（不动的部分）
    { "block": "cube", "pos": [x,y,z], "size": [w,h,d], "material": "stone" }
    // block 可以是基础体（cube/stair/arch/ljoint...），原型期先用基础体拼
  ],
  "mechanisms": [
    {
      "id": "arch",
      "geometry": [ /* 属于这个机关、会一起动的几何，坐标相对 pivot */ ],
      "pivot": [x,y,z],
      "axis": "y",                      // 旋转轴
      "states": [0, 90],                // 允许的吸附态（度）
      "initial": 0,
      "handle": { "pos": [x,y,z] }      // 玩家拖拽的手柄位置
    }
  ],
  "nodes": [                            // 角色能站的点
    { "id": "start", "pos": [x,y,z] },
    { "id": "mid",   "pos": [x,y,z] },
    { "id": "exit",  "pos": [x,y,z] }
  ],
  "edges": [                            // 可行走连接；requires 为空=恒有；否则需机关处于指定状态
    { "a": "start", "b": "mid",  "requires": {} },
    { "a": "mid",   "b": "exit", "requires": { "arch": 90 } }
  ],
  "start": "start",
  "exit": "exit"
}
```

### 完整示例：〇·门

**这就是整套骗术的最小体现** —— `mid → exit` 这条边只有当拱券 `arch` 旋到 90° 时才激活：3D 里那两段本不相连，但旋到 90° 后在相机视角下对齐成一道可走的门洞。

```json
{
  "id": "00_gate",
  "camera": { "azimuthDeg": 45, "elevationDeg": 35.264 },
  "palette": "ink",
  "geometry": [
    { "block": "cube", "pos": [0, 0, 0], "size": [2, 1, 2], "material": "stone" },
    { "block": "cube", "pos": [4, 2, 0], "size": [2, 1, 2], "material": "stone" }
  ],
  "mechanisms": [
    {
      "id": "arch",
      "geometry": [ { "block": "arch", "pos": [0,0,0], "size": [2,3,1], "material": "stone" } ],
      "pivot": [2, 1, 0],
      "axis": "y",
      "states": [0, 90],
      "initial": 0,
      "handle": { "pos": [2, 3.2, 0] }
    }
  ],
  "nodes": [
    { "id": "start", "pos": [0, 1, 0] },
    { "id": "mid",   "pos": [2, 1, 0] },
    { "id": "exit",  "pos": [4, 3, 0] }
  ],
  "edges": [
    { "a": "start", "b": "mid",  "requires": {} },
    { "a": "mid",   "b": "exit", "requires": { "arch": 90 } }
  ],
  "start": "start",
  "exit": "exit"
}
```

> 坐标是示意值，具体由关卡设计者在编辑/调试时对齐到"屏幕上接上"为准。建议给运行时加一个 debug 叠层：显示所有 node、当前激活 edge、机关当前 state，方便 Sally 手工调关。

---

## 5. 建议文件结构

```
molt/
├─ index.html
├─ vite.config.js
├─ src/
│  ├─ main.js                # 启动、场景、相机、渲染循环
│  ├─ engine/
│  │  ├─ Level.js            # 加载关卡数据、建几何、建机关
│  │  ├─ Navigation.js       # 节点图、依状态算激活 edge、寻路(BFS)
│  │  ├─ Character.js        # 角色、沿路径走动画、瞬移接缝
│  │  ├─ Mechanism.js        # 机关：吸附态、旋转补间、手柄拖拽
│  │  ├─ Interaction.js      # 射线拾取：点节点 / 拖手柄
│  │  └─ Highlight.js        # 可达节点高亮（朱砂）
│  ├─ blocks/                # 基础体几何工厂 (cube/stair/arch/ljoint...)
│  ├─ render/
│  │  ├─ camera.js           # 正交等距相机
│  │  └─ lighting.js         # 方向光+环境光+AO（无点光源）
│  ├─ tokens.js              # 从 Design 拿来的色彩/材质/字体值
│  └─ levels/
│     ├─ 00_gate.json
│     ├─ 01_burden.json
│     └─ ...
└─ public/                   # 贴图/字体等静态资产
```

---

## 6. 构建顺序（里程碑 + 验收标准）

**M0 — 引擎骨架（灰模）**。先不碰美术，用灰盒子把 〇·门 跑通。
验收：正交等距相机就位；能拖手柄把拱券在 0°/90° 间吸附旋转；旋到 90° 时 `mid→exit` 边激活、`exit` 节点亮起可达高亮；点 `exit`，角色从 `start` 寻路走到 `exit`（含接缝瞬移）并触发胜利。debug 叠层可见 node/edge/state。

**M1 — 首个套皮可玩关**。拿 Design 的 `tokens.js` + 门的视觉规格，把 〇·门 套上墨色水墨风、朱砂交互信号、负重黑影角色。
验收：门看起来是"成品级"的一屏水墨，可玩闭环完整；无点光源穿帮；朱砂只出现在可交互/出口处。

**M2 — 铺 MVP 关卡**。逐个实现 一·负重、二·漏窗、五·大成若缺、七·水（+可选 八·平衡）。每关可能引入一种新机关类型（配重、抽棂条、留缺口、松手静置水流、45°中脊），按需扩展引擎但保持数据驱动。
验收：MVP 5（+1）关全部可通关；难度正向递增；"无为家族"关卡（漏窗/大成若缺/水）的**提示引导**能让新玩家不看攻略也能想到"反直觉解法"（尤其"水"关：操作使其更糟、静置使其变好的因果要肉眼可见）。

---

## 7. 与 Claude Design 的接口

- 你消费 Design 交付、经 Sally 带进来的：`tokens.js`（**具体的十六进制色值、材质名、字号/字体、间距**）+ 每关的建筑/氛围视觉规格 + 导出的网格/贴图（如有）。
- 把这些集中在 `src/tokens.js` 与 `public/`，**不要把颜色/字号硬编码散落各处**，方便 Design 迭代时一处替换。
- 你产出给 Sally 的是可玩构建的截图/录屏，她带回给 Design 做评审。

---

## 8. 明确不做（v1 out of scope）

- 全部 12 关（本期只做 MVP 5–6 关）。
- 原生移动端 / App 商店打包（先 Web）。
- 复杂存档系统（原型期一个"已解锁到第几关"即可）。
- 完整音频系统（可留占位；交互音效对手感重要，能加则加，但不是 M0 的事）。
- 触控手势精调（先鼠标；触控放阶段 2 之后）。
- 关卡编辑器 GUI（Sally 直接编辑 JSON + debug 叠层即可；若后期关卡多了再考虑）。

---

## 9. 给实现者的一句提醒

这个项目最容易翻车的不是渲染，是**导航**——"屏幕相邻≠空间相邻"。把 §3 的"状态条件边（state-conditional edge）"这条吃透，其余都是常规活。先用 §4 的 门 示例把这条链路打通，再谈别的。
